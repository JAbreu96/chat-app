{
  "name": "chat-app",
  "description": "Chat Application",
  "scripts": {
    "start": "nodemon --watch src -e ts,ejs  --exec npm run build",
    "build": "tsc --project ./ && node ./src/index.js"
  },
  "dependencies": {
    "sequelize": "^6.12.0-alpha.1",
    "socket.io": "^4.4.0"
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.2",
    "@types/express": "^4.17.13",
    "express": "^4.17.1",
    "nodemon": "^2.0.15"
  }
}